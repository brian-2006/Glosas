
services:

  db:
    image: postgres:16

    container_name: database

    restart: always

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 3118074248
      POSTGRES_DB: glosas_db

    ports:
      - "5433:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./BackEnd

    container_name: glosas_backend

    restart: always

    ports:
      - "8000:8000"

    depends_on:
      - db

    volumes:
      - ./BackEnd:/BackEnd

    env_file:
      - ./BackEnd/.env

  frontend:
    build: ./FrontEnd

    container_name: glosas_frontend

    restart: always

    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --force"

    environment:
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"

    ports:
      - "5173:5173"

    depends_on:
      - backend

    volumes:
      - ./FrontEnd:/FrontEnd
      - /FrontEnd/node_modules

    env_file:
      - ./FrontEnd/.env

volumes:
  postgres_data:
